# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@leio-ai/leio-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@leio-ai/leio-lsp'
          - id: lsp-stdio
            name: '@leio-ai/leio-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@leio-ai/leio-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@leio-ai/leio-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@leio-ai/leio-llm-replay'
