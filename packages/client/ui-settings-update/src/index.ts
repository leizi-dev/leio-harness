/** Host loader entry for the Electron-only browser implementation exported from `./client`. */

/** This package has no host-side behavior; the desktop main process owns updates. */
export function apply(): void {}
